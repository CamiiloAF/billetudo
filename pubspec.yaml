name: finance_app
description: App de finanzas personales local-first (Flutter). Nombre placeholder — renómbralo.
publish_to: 'none'
version: 0.1.0+1

# NOTA: las versiones son indicativas. Ajusta con:
#   flutter pub get   /   flutter pub upgrade --major-versions
environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Estado (decisión: bloc/cubit) ---
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  equatable: ^2.0.5

  # --- Base de datos local (offline-first, fuente de verdad) ---
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  path: ^1.9.0
  uuid: ^4.4.0

  # --- Sync + backend (decisión: PowerSync + Supabase) ---
  powersync: ^1.9.0
  supabase_flutter: ^2.5.6

  # --- Gráficas ---
  fl_chart: ^0.68.0

  # --- Monetización (rewarded ads + suscripciones) ---
  google_mobile_ads: ^5.1.0
  purchases_flutter: ^6.29.0

  # --- Captura sin fricción ---
  speech_to_text: ^6.6.2
  google_mlkit_text_recognition: ^0.13.0

  # --- Utilidades ---
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Codegen de Drift
  drift_dev: ^2.18.0
  build_runner: ^2.4.11

  # Testing (piramide: unit + cubit + mocks + e2e)
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
  patrol: ^3.11.0 # e2e; requiere `dart pub global activate patrol_cli` y setup nativo tras `flutter create .`

flutter:
  uses-material-design: true
  generate: true # habilita l10n si usas flutter gen-l10n

dependency_overrides:
  # path_provider_foundation >=2.5 migró a native assets (hook/build.dart),
  # lo que rompe la compilación del build script de build_runner en este SDK.
  # Fijar a la última versión pre-hooks hasta que build_runner soporte hooks.
  path_provider_foundation: 2.4.2
