name: billetudo_lints
description: Custom analyzer rules enforcing the billetudo code conventions.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  analyzer: ^8.0.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.8.1

dev_dependencies:
  flutter_lints: ^4.0.0
