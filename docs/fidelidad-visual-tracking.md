# Track de fidelidad visual contra Pencil

Estado de la auditoría de fidelidad (`/design-fidelity-check` vía `pencil-fidelity-reviewer`, o
la auditoría manual equivalente) por feature, contra `billetudo.pen`. Actualizar esta tabla
cada vez que se cierre o se agende una pasada — es el único lugar que consolida el estado,
los `docs/dev-runs/*.md` quedan como el detalle de cada corrida.

## Cómo leer la tabla

- **Estado**: ✅ Aprobada · 🟡 Parcial (cubre parte de la pantalla o quedó pendiente de re-verificación) · ⏳ Agendada (hay prompt/plan listo, no corrida) · ❌ Sin auditar · ⬜️ N/A (feature sin UI aún o sin frame en Pencil)
- **Goldens**: si la feature tiene cobertura golden (`test/features/<feature>/presentation/golden/`), prerrequisito técnico para que `pencil-fidelity-reviewer` pueda comparar sin emulador.
- **Fuente**: el dev-run o documento donde se registró el resultado.

| Feature | Estado | Fecha | Goldens | Fuente | Notas |
|---|---|---|---|---|---|
| Cuentas | ✅ Aprobada | 2026-07-21 | ✅ (`test/features/accounts/.../golden`, +58 goldens) | `docs/dev-runs/fidelidad-cuentas.md` + `docs/dev-runs/mejoras-carrusel-saldo-y-ajuste-saldo.md` | Primer cierre formal vía `/design-fidelity-check` (antes solo auditoría pixel-a-pixel manual del 2026-07-19). 3 rondas: 9 hallazgos (formato de dinero, botones Guardar ausentes, skeleton, labels, sheets con mensaje único, campo condicional) + 2 nuevos en ronda 2 (campo "Tasa de interés" ausente para cuentas no-tarjeta — CRÍTICO real, contradecía el diseño aprobado; botón "Archivadas" propagado a 6 frames de Pencil que no lo tenían). Todo corregido y re-verificado sin hallazgos pendientes. **Adición ajuste de saldo (Mejora #1, 2026-07-21):** lápiz de ajuste en el detalle (normal `c2jrG`/`r0hXJd`, tarjeta `Uk8DL`/`bT7Ga`), campo "Deuda actual" al crear tarjeta (`XcEBG`/`Dyfwj`), ausencia de "Saldo inicial" al editar, y hoja "Ajustar saldo" (`s0c82`/`t42NgN`, 6 goldens) — todo **fiel** en claro y oscuro. 1 MENOR (valor crudo en el `Form Field` editable = artefacto del golden, el `MoneyInputFormatter` sí formatea en la app) + MENOR pre-existente ajeno a la mejora (label "Número de cuenta" vs "Número de tarjeta" en el detalle de tarjeta). |
| Pagos programados | ✅ Aprobada | 2026-07-20 | ✅ (`test/features/scheduled_payments/.../golden`) | `docs/dev-runs/pagos-programados.md` (sección "Fidelidad visual — historial de correcciones") | 7 rondas: 4 de auditoría/fix inicial + color por categoría (alcance ampliado a todo el sistema) + unificación de componentes compartidos con Transacciones. Cierre más completo que existe hasta hoy. **CTA "Confirmar ahora" (2026-07-20)**: cierra el punto 1 de `docs/bugfixes.md`, que había quedado sin implementar pese a estar mal marcado como resuelto. 2 variantes propuestas → elegida CTA junto al Hero → ícono `zap` elegido por `ui-ux-reviewer` (evita colisión semántica con `check-circle`/`calendar-check`, ya usados en la misma feature para otros significados) → claro + oscuro sobre los frames reales de producción. 1 hallazgo MENOR de `pencil-fidelity-reviewer` (gap 12px vs 10px del token), corregido y re-verificado. |
| Categorías | ✅ Aprobada | 2026-07-20 | ✅ (`test/features/categories/.../golden`, 156 tests) | `docs/dev-runs/fidelidad-categorias.md` | 2 rondas: 7 hallazgos (Page Header ausente en 8 pantallas, Segmented Control reconstruido a mano, sheet de borrado simple con rojo punitivo, título extra no diseñado en 2 sheets, link/label/subtítulo condicionales faltantes) + re-verificación sin hallazgos pendientes. Bloqueo de catálogo de íconos (15/32) **cerrado**: confirmado idéntico al `.pen` vigente (64 íconos), ya no diverge. Gap no bloqueante: picker de categoría padre sin frame de loading/empty en Pencil. |
| Presupuestos | ✅ Aprobada | 2026-07-21 | ✅ (`test/features/budgets/.../golden`, 98 goldens, todos verificados) | `docs/dev-runs/fidelidad-presupuestos.md` + `docs/dev-runs/presupuestos-ajuste-un-periodo.md` | La sospecha de deriva amplia (prompt previo, referencia Pagos Programados 11 críticos) **no se confirmó**: solo 1 hallazgo IMPORTANTE (subtítulo truncado del Error State compartido `accountsErrorLocalFirst`), corregido — el fix cascadeó a Cuentas y Categorías, que tenían el mismo texto recortado. Sin CRÍTICOS; acción destructiva reversible ya en violeta neutral (no rojo), `AppFab` del sistema en uso. Gaps no bloqueantes: Presupuestos no tiene ninguna instancia de `Error State` colocada en el `.pen` (a diferencia de otras features), varios estados sin fila propia en `presupuestos.md`. Hallazgo colateral fuera de alcance: `transactionsErrorLocalFirst` usa redacción distinta al resto — pendiente para una futura pasada sobre Transacciones. **Ronda adicional (ajuste de monto por 1 período, 2026-07-20)**: 4 piezas nuevas verificadas contra Pencil (`JR1Xp`/`Z72OIH`, `A8ZfHd`/`D0EoN`, `k6fKsZ`/`PPzUv`, `reulY`/`ujbZf`) — 1 hallazgo IMPORTANTE (botón "Aplicar cambios" envolvía a 2 líneas en el modo editar/cancelar), corregido con `FittedBox` y re-verificado en goldens; el resto fiel al píxel. **Ronda 2026-07-21 (`/design-fidelity-check budgets`, tras la sesión de mejoras UX + validaciones)**: fidelidad de código **APROBADA** en las 4 pantallas y todos los sheets (claro+oscuro), cero CRÍTICO/IMPORTANTE. Verificados al píxel: par de cards Programado+Ajuste como instancias de `Entry Row` `s09qcC`, banner con copy relativo al período ("Ajuste de monto" / "$X · [rango]"), sheet de ajuste con "Revertir ajuste" + token `hintText`. Tras compilar `lib/` se cerró el bloqueo: **`budget_form_page` (16 goldens) verificado FIEL** (CTA siempre activa incluida) y se agregó el golden de coexistencia Programado+Ajuste (`detail_scheduled_and_adjustment`, claro/oscuro), también FIEL contra `reulY`/`ujbZf`. El frame stale `JR1Xp`/`Z72OIH` se corrigió en Pencil ("próximo período"→"este período"). `pencil-fidelity-reviewer`: **cero hallazgos en las 18 superficies**. Único gap residual **no bloqueante**: el sheet info "¿Qué es el modo sobres?" (`eBwb0`/`gAetG`) aún sin golden dedicado. **Corrida 2026-08-04 (`budget-income-counts-in-budget`): fidelidad N/A, no auditada.** `design-system/billetudo/pages/budgets.md` no existe, así que no hay tabla Pantalla/pieza → Node ID para mapear los goldens de `test/features/budgets/presentation/golden/goldens/*.png` (incluido el signo `+` nuevo de `BudgetActivityRow` para income presupuestable) contra `billetudo.pen`; no es fallo de acceso, es que la feature aún no tiene ese documento de diseño. El toggle "Incluir en tu presupuesto" extendido a `type=income` en `TransactionFormPage` tampoco tiene frame propio en Pencil (`transacciones.md` solo documenta la variante de transferencia) — se reusó el componente/copy genéricos tal cual. Estado se mantiene ✅ Aprobada por la auditoría real ya cerrada el 2026-07-21; pendiente: generar `design-system/billetudo/pages/budgets.md` antes de poder auditar esta pieza y cualquier cambio futuro de forma no ambigua. Ver `docs/dev-runs/budget-income-counts-in-budget.md`. |
| Transacciones | ✅ Aprobada | 2026-08-04 | ✅ (`test/features/transactions/.../golden`, incluye goldens nuevos del chip/hoja Presupuesto) | `docs/dev-runs/mejoras-carrusel-saldo-y-ajuste-saldo.md` + `docs/dev-runs/fidelidad-transacciones.md` + `docs/dev-runs/movements-budget-period-chip.md` | Base ✅ desde 2026-07-20 (3 rondas: 9 hallazgos + bug de nota/categoría + 2 gaps de implementación reales + cierre; ver fidelidad-transacciones.md). **Adición carrusel de saldo (Mejora #2, 2026-07-21):** el componente `C2g9cA` es **fiel** en claro y oscuro (`cgasM`/`Y0lWi`); fix de contraste de la deuda a `$expense-text`. Pasada a 🟡 Parcial porque la auditoría surfaceó **2 hallazgos IMPORTANTE en el chrome compartido de Movimientos, PRE-EXISTENTES al carrusel** (no los introduce la mejora): (1) título "Movimientos" centrado en el código vs alineado a la izquierda en Pencil; (2) chip de **Fecha en 2º lugar** en el código vs **último** en la spec §3 (`Cuenta → Categoría → Tipo → Etiqueta → Fecha`). Pendientes de decidir/corregir (decisión aparte). Gaps de cobertura: estados **colapsado** (`rGVw1`/`uxIps`) y **tarjeta-activa** (`Ljf8l`/`RdbCG`) del carrusel sin golden propio. **Corrida 2026-08-04 (`movements-budget-period-chip`): fidelidad N/A, no auditada.** `design-system/billetudo/pages/transactions.md` sigue sin existir, así que no hay tabla Pantalla/pieza → Node ID para mapear el 6to `FilterChipPill` "Presupuesto" ni su hoja de selección (ni ninguno de los 102 goldens de la feature) contra `billetudo.pen`; no se llamó a Pencil porque el gap de documentación ya bloquea la auditoría antes de necesitarlo. El chip y la hoja se compusieron solo con componentes ya aprobados (`FilterChipPill`, `BottomSheetBase`, `SheetHead`, `SheetButtonsRow`, fila estilo `AccountSelectRow`), sin frame propio. Estado se mantiene 🟡 Parcial por los 2 hallazgos IMPORTANTE pre-existentes del carrusel (sin corregir). Pendiente que se acumula: generar `design-system/billetudo/pages/transactions.md` antes de poder auditar esta y futuras piezas de Transacciones de forma no ambigua. Ver `docs/dev-runs/movements-budget-period-chip.md`. **Corrida `/design-fidelity-check transacciones` (2026-08-04), esta vez con acceso real a Pencil:** auditados los 102 goldens de la feature contra `transacciones.md`. **Cero CRÍTICO/IMPORTANTE** en las 4 áreas priorizadas — todas fieles: (1) handle de colapso compartido del carrusel (`rGVw1`/`uxIps`/`d2TX3`), (2) chip/sheet "Presupuesto" con los dos fixes de contraste previos (icon-wrap circular 44×44, check en `colors.primaryOnSoft`) confirmados aplicados en el golden, (3) badge de grupo con total al filtrar por un solo tipo, (4) campo de nota vía `NoteAutocompleteField` fiel a `taqc1`/`LOnm3` (sin ícono, igual que la instancia real `UcZSx`). Nota positiva: el hallazgo IMPORTANTE de título "Movimientos" descentrado ya **no se observa** en `transactions_page_with_data_*` — parece corregido, pendiente de confirmar si el de orden del chip Fecha (2º vs. último en la spec) sigue vigente. Barrido adicional sobre sort popover, transferencia presupuestable, rango de fecha, sheets de pago programado y detalle mínimo, sin hallazgos. **Gaps de cobertura nuevos, solo documentación:** el comportamiento del badge-con-total no está en `transacciones.md`; 3 sheets (`sheet_future_date_scheduled_payment_prompt`, `sheet_edit_impact_warning_scheduled_only`, `sheet_date_range_picker`) tienen golden pero no fila propia en el `.md`; varios componentes reusables (`Rslzk`/`ofg07`, `npfLO`, `jt8dk`, `nM9ea`, `X3tZG`, `zSTlU`, `H5mzN`) no tienen golden aislado, solo embebidos. **Cierre a ✅ Aprobada (2026-08-04):** los dos pendientes que mantenían la fila en 🟡 Parcial quedaron resueltos, no solo documentados:
1. **Gaps de documentación cerrados en `transacciones.md`**: badge de grupo con total documentado (nueva sub-sección en "Lista de transacciones"), los 3 sheets sin fila propia (`sheet_date_range_picker` = mismo componente que `OFdj4`/`mi0hB`, no uno nuevo; `sheet_edit_impact_warning_scheduled_only` = variante de copy de `L9DJI`; `sheet_future_date_scheduled_payment_prompt` = cruce con Pagos Programados) anotados explícitamente, y las fidelidades del handle de carrusel y del chip/sheet Presupuesto marcadas como cerradas en sus secciones respectivas.
2. **El hallazgo IMPORTANTE "chip Fecha en 2º lugar vs. último" resultó ser un falso positivo, no una divergencia real**: comparaba contra el orden prosa (no normativo) de `docs/requirements/03-transacciones.md`, mientras que la propia `transacciones.md` (línea 82) ya registra la decisión DEFINITIVA del usuario del 2026-07-21 con Fecha en 2º lugar — el código ya coincide con esa decisión al pixel. Verificado leyendo `TransactionsFilterBar` en `transactions_page.dart`: orden real Cuenta→Fecha→Categoría→Tipo→Etiqueta, idéntico al documentado.
3. **Causa raíz sistémica del "gap `pages/transactions.md`" corregida en la fuente**, no solo parcheada aquí: `.claude/workflows/feature-dev.js` (fase Fidelity) buscaba el spec de cada feature como `pages/${featureDir}.md` usando el nombre inglés de la carpeta de `lib/features/` — pero **todos** los `.md` de `design-system/billetudo/pages/` están en español (`transacciones.md`, `metas.md`, `presupuestos.md`, etc.), así que esa búsqueda fallaba sistemáticamente para toda feature nueva o corrida futura (mismo patrón ya visto en las filas de Metas, Presupuestos, Deudas de esta tabla). Corregido el prompt del agente de fidelidad para que haga `Glob` sobre `pages/*.md` y resuelva el archivo correcto semánticamente, en vez de asumir el nombre inglés — beneficia a todas las features futuras, no solo a Transacciones.

Sin hallazgos CRÍTICO/IMPORTANTE pendientes y sin gaps de cobertura reales tras esta corrida (el resto de notas de gaps arriba son solo de documentación, ya cerradas). Ver este reporte, `docs/dev-runs/movements-budget-period-chip.md` y `.claude/workflows/feature-dev.js`. |
| Deudas | 🟡 Parcial | 2026-07-22 | ✅ (`test/features/debts/.../golden`, 40 goldens + cross-link en `scheduled_payments`) | esta corrida (implementación completa de Deudas + `/design-fidelity-check deudas`) | Feature nueva de punta a punta (esquema v14, domain/data ledger, presentación: lista, detalle, form, abono Sí/No + enlazar movimiento existente, actualizar saldo, config de cuota, cross-link con Pagos Programados). Fidelidad **alta, cero CRÍTICO**. **1 IMPORTANTE corregido:** la `Debt Card` de la lista omitía el badge "Cuota · <fecha>"/"Vence <fecha>" (`xSpw7`) → `WatchDebts` ahora expone la cuota enlazada y el badge se renderiza. Minors **corregidos**: caret-gota del hero (`enableInteractiveSelection:false`), skeleton del detalle con placeholder de card de cuota. Minors **aceptados**: header de error sin título (deuda no cargó), Tab Bar presente en modo enlazar (pantalla apilada, defendible), Category Quick Picker con solo "Otra" en el golden de cuota (fixture, no deriva). **Gaps de cobertura (no bloqueantes):** `DebtConfigureInstallmentCard` (estado sin-cuota) y detalle "saldada" no tienen frame en Pencil (por diseño); modo enlazar (`g0x859`), sheet confirmar-borrado y pickers de cuenta/moneda sin fila propia en `deudas.md`. **Patrol e2e** (17 escenarios en 2 suites) escritos y compilando; corrida en emulador `dev` en curso. |
| Metas | ✅ Aprobada | 2026-07-29 | ✅ (`test/features/goals/.../golden`, 124 goldens) | corridas `/design-fidelity-check goals` del 2026-07-28 (2 pasadas) + re-verificación puntual + `docs/dev-runs/goals-name-field-and-quick-amount-chips-fix.md` (2026-07-29) | **Corrida 2026-07-29 (`goals-name-field-and-quick-amount-chips-fix`): fidelidad N/A, no auditada.** `design-system/billetudo/pages/goals.md` no existe (`Read` confirma "File does not exist"), así que no hay tabla Pantalla/pieza → Node ID que permita mapear los 124 goldens actuales a nodeIds concretos del `.pen` de forma no ambigua — en particular los dos pares `goal_detail_page_quick_amount_prefilled_sheet_light/dark` y `goal_detail_page_quick_amount_prefilled_custom_sheet_light/dark` tocados en esta corrida. Se confirmó acceso real al `.pen` (`get_editor_state` respondió con el frame activo y 130 componentes `reusable:true`), así que el bloqueo es de documentación, no de acceso. Estado se mantiene ✅ Aprobada por la auditoría real ya cerrada el 2026-07-28 (ver debajo); pendiente: generar `design-system/billetudo/pages/goals.md` (tarea de `pencil-designer`/`ui-ux-reviewer`) antes de poder auditar esta pieza y cualquier cambio futuro contra Pencil de forma no ambigua.**Primera pasada:** deriva visual real y significativa, no detectada por los golden tests en sí (solo verifican regresión contra sí mismos, no fidelidad contra Pencil) — confirma el riesgo que describe CLAUDE.md sobre Pagos Programados. 7 CRÍTICO + 11 IMPORTANTE + 4 MENOR + ~20 estados sin golden. **Todo corregido en esta sesión:** header sin flecha atrás en `goals_list_page` (raíz de tab); empty-state con hero de gradiente real (`qzBkN`) en vez del genérico; selector de ícono agregado al formulario (`PjBCt`/sheet `cwj6B`) con prefill completo desde plantillas (nombre+ícono+monto sugerido); arco héroe del detalle con "%" (ícono se conserva en el mini-arco de lista, por diseño); `archived_goal_row` reconstruida con mini-arco/objetivo/fecha/badge "Cumplida"; toggle "¿Mover dinero de una cuenta?" implementado completo en modo ON (picker de cuenta vía `Account Select Sheet`, `Category Quick Picker`, gate si la meta no tiene cuenta) tras confirmar en Pencil que el frame sí estaba aprobado (10 frames verificados: `BETeo`/`GK69y`/`CpLy8`/`DiDwa`/`jc77R`); los 11 IMPORTANTE y 4 MENOR restantes corregidos (flecha vs X en header del form, jerarquía de campos, textos de ayuda, orden CTA/chips, íconos de movimiento, subtítulos de sheet de acciones, banner DEBUG en goldens, etc). **4 gaps de funcionalidad real implementados** (no solo golden, código nuevo): filtro por cuenta in-place en la lista (desde el link de la señal de coherencia HU-12), indicador de cuenta con lápida en el detalle, sheet de detalle de movimiento (`GoalMovementDetailSheet`), sheet de eliminar movimiento con 3 variantes de copy (transferencia/manual/meta cumplida) — los 4 confirmados contra frames reales y aprobados antes de construir. **Segunda pasada (re-verificación):** 6/7 CRÍTICO confirmados resueltos por pixel; 1 hallazgo IMPORTANTE nuevo (CTA "Ajustar la fecha" ausente en el estado de fecha vencida, `Mnc0Q`) — corregido y re-verificado. Cobertura completada: archivada+cumplida (badge visible) y retirar mover-ON con presupuesto. **Gap remanente, esperado (no defecto):** integración con Pagos Programados (HU-16) sin implementar — excluida explícitamente del alcance desde el plan original de esta feature; queda como iniciativa aparte. **Adición "Aporte rápido personalizable" (2026-07-28, tras feedback de uso real):** reporte de usuario en dispositivo físico detectó 4 bugs reales que ningún golden atrapó (nombre desalineado en el form por `counterText`/falta `textAlignVertical`; selector de fecha objetivo marcando una fecha fabricada como "elegida" sin resaltarla en el calendario; anillo de progreso invisible en oscuro por `$track` mal calibrado — bug real en el propio `.pen`, corregido de raíz de `#101018` a `#2A2A3D`; toggle "¿mover dinero?" sin estado deshabilitado visual pese a ser un no-op silencioso) — los 4 corregidos. Además, dos cambios de comportamiento pedidos por el usuario (chips de monto abren el sheet prefilled en vez de commitear directo; "Otro" pasa a crear chips personalizados persistentes) **contradecían el diseño ya aprobado** — se pausó y se llevó a `pencil-designer` antes de construir (esquema Drift nuevo `GoalQuickAmounts` + tabla Supabase-dev). Auditoría de fidelidad de la pieza nueva: 1 IMPORTANTE (ícono de lápiz obsoleto en el chip "Otro monto", heredado de un nodo de Pencil ya reemplazado) + 1 gap de cobertura (golden del flujo real chip→sheet prefilled) — ambos corregidos y re-verificados. `dart analyze` limpio, 268/268 tests en verde. **Corrida 2026-07-29 (`goal-contribution-hide-move-funds-toggle`):** N/A, feature sin UI nueva — cambio de lógica de visibilidad (ocultar el toggle "mover dinero" cuando la meta no tiene cuenta vinculada, en vez de mostrarlo deshabilitado) sobre una pantalla ya aprobada, sin tocar ningún frame de Pencil; ver `docs/dev-runs/goal-contribution-hide-move-funds-toggle.md`. Estado ✅ Aprobada se mantiene. **Corrida 2026-07-29 (`goal-movement-edit-tracking-only`): fidelidad N/A, no auditada.** Misma causa raíz que la corrida anterior del mismo día: `design-system/billetudo/pages/goals.md` sigue sin existir, así que no hay tabla Pantalla/pieza → Node ID para mapear los 4 goldens nuevos (`sheet_edit_goal_movement_{light,dark}`, `sheet_edit_goal_movement_error_{light,dark}`) ni el par actualizado (`sheet_goal_movement_detail_manual_{light,dark}`) contra `billetudo.pen`. `EditGoalMovementSheet` (hoja nueva de esta corrida) se compuso solo con componentes ya aprobados (`GoalAmountHeroField`, `GoalMovementFieldLabel`, `DatePickerSheet`, campo de nota, `DetailActionsRow`), sin frame propio en Pencil — pendiente de validación visual formal cuando exista `goals.md`. Estado se mantiene ✅ Aprobada por la auditoría real ya cerrada el 2026-07-28; el pendiente de documentar `design-system/billetudo/pages/goals.md` sigue abierto y ahora cubre también esta pieza. Ver `docs/dev-runs/goal-movement-edit-tracking-only.md`. **Corrida 2026-07-29 (`goal-withdraw-usar-todo-fix`): fidelidad N/A, no auditada.** Misma causa raíz que las dos corridas anteriores del mismo día: `design-system/billetudo/pages/goals.md` sigue sin existir, así que no hay tabla Pantalla/pieza → Node ID para mapear los goldens tocados (`sheet_goal_withdraw_*` y `goal_detail_page_quick_amount_prefilled_*`, claro/oscuro) contra `billetudo.pen`. El fix separó el texto pasivo "Disponible en la meta" del CTA tapeable "Usar todo" (antes un solo `TextButton`) y corrigió que `GoalAmountHeroField` no reflejara visualmente el monto tras tocar "Usar todo"; el estilo del CTA (subrayado + `primaryOnSoftStrong`) se tomó por consistencia con "Enlazar un movimiento" en el mismo archivo, sin spec que lo respalde. Estado se mantiene ✅ Aprobada por la auditoría real ya cerrada el 2026-07-28; el pendiente de documentar `design-system/billetudo/pages/goals.md` sigue abierto y ahora cubre también esta pieza. Ver `docs/dev-runs/goal-withdraw-usar-todo-fix.md`. **Corrida 2026-07-29 (`edit-goal-movement-amount-error`): APROBADA — 0 hallazgos.** El fix ancló el error de guardado de "Editar movimiento" al campo de Monto (`GoalAmountHeroField.errorText`, borde `$expense` + mensaje) para `ValidationFailure` de `fieldAmountMinor`, reutilizando el patrón ya aprobado de `GoalContributionSheet`; el texto genérico suelto se conserva solo para fallas no atribuibles al monto. Sin hallazgos ni gaps de cobertura — la pieza compone únicamente componentes ya aprobados, sin frame propio nuevo. `design-system/billetudo/pages/goals.md` sigue sin existir (mismo pendiente abierto de las corridas anteriores del día). Ver `docs/dev-runs/edit-goal-movement-amount-error.md`. |
| Home / Dashboard (incluye "Más") | 🟡 Parcial | 2026-07-20 | ✅ (`test/features/home/.../golden`, 125 tests) | `docs/dev-runs/fidelidad-home.md` | 3 rondas: 9 hallazgos (formato de dinero, signo, header condicional, MoreRow sin descripción, fila "Gráficas e informes"+badges agregada al `.pen`, íconos/borde/truncado de tab bar) + 4 hallazgos nuevos en ronda 2 (2 íconos de tab bar, truncado en filas "Próximamente", copy del selector de mes) + causa raíz real del truncado en ronda 3 (chevron/gap/badge robando ancho). Todo corregido y re-verificado salvo un ítem diferido a propósito: **estado "con presupuesto" del hero (barra de progreso) sigue sin implementar**, decisión explícita del usuario. De paso se corrigió un crash real (`state.spending!` en primera carga fallida). |
| Auth (login/merge/HU-06/HU-07) | ✅ Aprobada | 2026-07-20 | ✅ (`test/features/auth/.../goldens`, 28) | corrida `/design-fidelity-check auth` del 2026-07-20 (2 pasadas) | Fiel a Pencil en todo lo implementado, re-verificado por `pencil-fidelity-reviewer` tras corregir. Primera pasada: HU-06 limpia, 9 hallazgos en login/borrar-cuenta. Segunda pasada (commit `6ffc672`): los 9 cerrados (Icon Header de `K8SAG` con ícono `phone`, label de Google en carga, "Eliminar cuenta" en una línea vía `FittedBox`, "Reintentar" primario, "Continuar" violeta 0.4 + `arrow-right`, y 3 copys igualados al `.pen`), HU-06 sin regresión. **Gaps de cobertura, no de fidelidad** (no bloquean): login iOS (botón nativo Apple, no capturable en golden), estados "borrando…"/carga sin frame por diseño. Ajustes y Más ya tienen código e implementación auditada por separado, ver filas "Configuración (Settings)" y "Home / Dashboard". |
| Configuración (Settings) | ✅ Aprobada | 2026-07-20 | ✅ (`test/features/settings/.../golden`, 66+ tests) | `docs/dev-runs/fidelidad-ajustes.md` + `design-system/billetudo/pages/ajustes.md` | Ítem 10 del backlog: el código ya existía (contra lo que decía el bugfix original), solo faltaba correr `/design-fidelity-check auth` de nuevo. 2 hallazgos IMPORTANTE (fila "Modo sobres" nunca compuesta en los frames reales de Ajustes, solo como referencia aislada — agregada al `.pen` en esta corrida en una sección nueva "Presupuesto"; sublabel "Claro" ausente en Apariencia) + 1 MENOR (avatar con 2 iniciales en vez de 1, corregido). Todo resuelto y re-verificado. Reviews sin hallazgos, incluida confirmación explícita de que "Eliminar cuenta" (HU-07) sigue accesible y sin gates. **Selector de tema (2026-07-20)**: primera página con su propio `ajustes.md`; reemplaza la fila "Apariencia" (chevron + "Próximamente") por card con `Segmented Control` Claro/Oscuro/Sistema, persistencia local por dispositivo (`shared_preferences`, primer uso en el proyecto, deliberadamente no sincronizada). Diseño (3 variantes → elegida A "segmented inline" → claro + oscuro, con/sin sesión) sin hallazgos bloqueantes de `ui-ux-reviewer`; `pencil-fidelity-reviewer` aprobó las 6 variantes de estado sin hallazgos nuevos. Gap MENOR heredado del componente base `Segmented Control` (contraste ~4.37:1 del label inactivo en claro, no introducido por esta pantalla) — ver punto 11 de `docs/bugfixes.md`. |
| Onboarding | ✅ Aprobada | 2026-07-30 | ✅ (`test/features/onboarding/presentation/golden/`, 12 goldens: 6 pantallas × claro/oscuro) | `docs/dev-runs/onboarding.md` | `/design-fidelity-check onboarding` completo, `pencil-fidelity-reviewer` con acceso real al `.pen` (vía `filePath` del worktree principal, mismo contenido/commit que el worktree `billetudo-onboarding`). Comparadas las 12 goldens contra sus nodeId (`fRrDQ`/`mmFVh`, `G7vDVK`/`c2wua2`, `O2QbEF`/`yClJt`, `MydOr`/`DfHXL`, `Gi0NV`/`Bylcp`, `bAKS6`/`ld3xh`). **1ª pasada:** 4 hallazgos IMPORTANTE, todos el mismo patrón — icono `coins` ausente en el campo "Moneda" e icono `banknote` ausente en "Saldo inicial"/"Deuda actual" de `first_account_page` (savings+card, claro+oscuro): el formulario compacto de Onboarding reusa `AccountFormField`/`AccountMoneyField` de Cuentas pero omitía el parámetro opcional `icon`. Corregido en `lib/features/onboarding/presentation/pages/first_account_page.dart` (3 sitios) y goldens regenerados. **2ª pasada (re-verificación): 0 hallazgos.** Nota resuelta, no reportada como hallazgo: el mockup de Pencil muestra "$0" en el campo de saldo, pero es el hint/placeholder de un campo vacío — con un valor real pre-cargado (`"0"`, como en Onboarding) ningún campo de dinero de la app antepone el símbolo, ni siquiera en Cuentas ya aprobado; comportamiento consistente en todo el sistema, no una divergencia. Sin gaps de cobertura (`.md` y goldens 1:1). |
| Splash | ✅ Aprobada | 2026-07-20 | ✅ (`test/features/splash/.../golden`, 2) | corrida `/design-fidelity-check splash` del 2026-07-20 | Feature nueva, una sola pantalla sin sheets/variantes. Fiel a Pencil en ambos temas (`bSOQb`/`raS94`) en la primera pasada — sin hallazgos `CRITICO`/`IMPORTANTE`. Único punto anotado fue de infraestructura del golden (spinner indeterminado capturado en frame t≈0, casi sin arco visible), corregido en la misma corrida con un `pump(300ms)` antes de la captura. |
| Sincronización (HU-08) | ✅ Aprobada | 2026-07-28 | ✅ (`test/core/sync/presentation/golden/` + hojas en `home`, 50 goldens) | corrida `/design-fidelity-check sincronizacion` del 2026-07-28 | Feature nacida del incidente de pérdida de datos (decisión #22 de `05-auth-sync.md`): 89 registros perdidos porque durante 3 días "sincronizando" se vio igual que una sincronización sana. **Cero CRÍTICO.** Los 5 chequeos de producto pasaron limpios: sin jerga técnica fuera del Registro técnico, sin ninguna acción "Descartar" en ninguna superficie, cero `$expense`, fila de tiempo en las 5 caras de la hoja, y la lista sin crecer con 89 pendientes (3 filas + "Ver los 89"). **12 defectos encontrados que ningún test existente detectaba** — el `analyze` estaba en 0 y 2706 tests en verde. Paso 1 (goldens nuevos, al mirarlos): 4 bugs del estado `stale` recién agregado — "Última sincronización **hace hace** 3 días"; copy mentiroso "Esos **0 cambios** viven solo aquí" (la rama de atención se colaba porque `isAttention` incluía `stale`); sección "Qué está esperando" vacía con encabezado huérfano; kicker que repetía la fila de tiempo. Paso 2 (contra Pencil): 3 IMPORTANTE + 5 MENOR, **todos corregidos**. El IMPORTANTE de fondo: **el umbral de 24 h se ignoraba en el estado de atención** — con la última sync hace 5 minutos la fila se pintaba igual de ámbar que con 3 días, reintroduciendo el problema del incidente dentro del propio estado de atención; el color pasa a derivarse de `SyncFreshness.isStale`, una sola fuente de verdad. Los otros dos: estado vacío de la lista completa sin composición (era un texto gris suelto, ahora `EmptyState` contra el frame nuevo `wcrqA`/`Z1ws4n`) y skeleton del encabezado estirado a 350px en vez de 150 (único de los 10 bloques que no calzaba). MENOR corregidos: segunda frase de la hoja Sincronizando, "dispositivo"→"teléfono", meta del detalle sin año (+ el `_asDate` sin `isUtc` que lo causaba), y copy propio del log vacío ("Aún no hay líneas · nada que reportar"). **2 frames creados en esta corrida** (`Pe8V9`/`f2roK0` estado `stale`, `wcrqA`/`Z1ws4n` vacío de la lista): el de `stale` se creó a propósito para validar el primer caso de la familia donde un `Button/Secondary` hueco se apoya sobre `$amber-soft` — medido, **se sostiene** (label 15.7:1 claro / 14.1:1 oscuro) y resulta mejor que el CTA inerte que la spec ya aceptaba, así que la regla de "jerarquía solo por peso" no se rompe. **Gaps de cobertura: ninguno** (18 pantallas del `.md` con golden en ambos temas). **Ronda 2 (2026-07-28, mismo día): los 2 residuales MENOR, cerrados.** (1) Formato del log: se acortaron los mensajes en el origen (`powersync_connector.dart`, `sync_quarantine_repository_impl.dart`) a estilo telegráfico ("push 12 ops", "retry #1", "stalled 20x/72h", "retry ok") en vez de prosa, y se quitó la duplicación de `tableName` que algunos mensajes repetían pese a que `toLogLine()` ya lo antepone — de 4/8 líneas visibles con la última cortada a la mitad, pasa a 6/8 completas sin cortes (2 siguen envolviendo a 2 renglones porque los nombres reales de evento son más largos que los placeholders del frame; aceptado, no vale forzar la taxonomía del dominio por estética). (2) La hoja del indicador del Home: al implementarla se encontró que el hallazgo original estaba incompleto — `HomeSyncStatus.attention` con cola vacía en realidad cubre **dos** causas distintas que el código conflaba en una sola copy. Con `SyncState.syncing` activo (reintento en curso) la copy de `W4oGp` ("Llevamos X días intentando subir tus cambios") es cierta y se conserva. Sin reintento activo y sin nada pendiente —el caso común— esa misma copy es **falsa** (afirma un intento que no existe): ahora tiene su propia rama con copy honesta ("Sin contacto con la nube", el mismo hecho que `syncHeroStaleTitle` de la pantalla principal). Detectado además que el test golden "too long" preexistente llevaba semanas ejercitando por accidente el caso silencioso (fixture con `state: synced`, no `syncing`) bajo el nombre y la copy equivocados — quedó renombrado a `stale` con la copy correcta, y se agregó un test nuevo para el caso `syncing` real. **Frame formal creado el mismo día** (`kvytN` claro / `iWlff` oscuro, variante de contenido sobre `W4oGp`/`G6yA34`): auditado por `ui-ux-reviewer` sin hallazgos (contraste, ausencia de `$expense`/violeta indebido, estructura idéntica al resto de la familia) — gap cerrado, tabla de `pages/sincronizacion.md` actualizada. 2709 tests no-golden en verde tras esta ronda. |
| Import/Export | ✅ Aprobada | 2026-08-06 | ✅ (`test/features/import_export/presentation/golden/`, 82+ goldens) | `/design-fidelity-check import_export` (2026-08-06) + re-verificación puntual del mismo día tras los fixes | **Cierre del ciclo completo de la misma corrida.** Auditoría inicial encontró 4 CRÍTICO + 8 IMPORTANTE + 2 MENOR (violeta en 3 `Empty State` + 1 CTA de error, `import_destinations_step` sin agrupar por sección, vista previa final sin header "Posibles duplicados (N)" ni sección de plantilla, `ExistingDestinationPickerSheet` sin reusar el selector de categorías, títulos/subtítulos divergentes, botón "Cancelar" ausente en Restaurar, preview en campos sin formato, texto de progreso recortado). Además, en revisión manual del usuario en dispositivo real aparecieron 2 hallazgos más: Resumen final con grilla de `StatChip` en vez de la tarjeta de filas etiqueta-valor del diseño (+ ícono `checkCheck` en vez de `check`), y etiquetas de los chips de Vista Previa desalineadas del `.pen` ("se importan/posibles duplicados/con error" vs "a importar/repetidos/inválidas"). **Los 10 puntos re-verificados el mismo día: 10/10 PASA.** Toggle Automático/Manual del paso de mapeo: deuda de diseño real (no existía en Pencil) cerrada en esta misma sesión — `pencil-designer` dibujó `drEA1`+toggle (Manual), `UuTCz` (Automático) y `HBdCo` (Automático + plantilla reconocida) en claro, aprobados por el usuario, luego generado el oscuro (`y19Ij`+toggle, `a1ESaS`, `lHG0E`); `ui-ux-reviewer` encontró 2 IMPORTANTE (banner ad-hoc en vez de reusar `Privacy Note Strip`, corregido; vacío estructural en modo Automático sin desglose por columna, resuelto por decisión explícita del usuario: "Automático solo con resumen"). Decisión documentada: "Mapear a existente" para categorías/subcategorías reusa `Category Select Sheet` (`SfSln`) tal cual; cuentas/etiquetas mantienen el picker genérico. Limpieza de higiene: 2 badges "🔖 EN REVISION" viejos sin cerrar de sesiones anteriores (`y19Ij`, `L82zyd`) detectados y eliminados. `pages/import-export.md` actualizado con los 3 nodeIds nuevos del toggle y la decisión del picker de categorías. Gaps de cobertura (documentación, no bloqueantes): 12 goldens sin fila propia en el `.md` (3 hojas de formato, picker de campo, hoja de omitidas, picker de destino existente, banner de plantilla reconocida, estado "todas inválidas", selección de archivo de Restaurar, estado "done" de Restaurar, progreso/error de Guardar copia, falla de historial) — quedan para una pasada de documentación aparte, no afectan el veredicto de fidelidad real. |
| Gráficas e Informes (Reportes) | 🟡 Parcial | 2026-07-30 | ✅ (`test/features/reports/.../golden`, +8 goldens de esta corrida: `categories_with_data`, `net_worth_loading`, `categories_loading`, `categories_selected`, `categories_subcategories` en claro+oscuro) | corrida `/design-fidelity-check graficas` del 2026-07-29 (2 pasadas) + `docs/dev-runs/graficas-cerrar-pendientes-categorias.md` (2026-07-30) + segunda corrida de fidelidad puntual (2026-07-30) | Feature nueva de punta a punta (Resumen/HU-04, Flujo/HU-01, Patrimonio/HU-02, Categorías/HU-03, sheet Selector de Periodo). Primera pasada: 2 CRÍTICO + 1 IMPORTANTE — (1) faltaba el Hero Resumen (stats row Presupuestos/Metas) al inicio de la pestaña Resumen con datos (`MKEqE`/`Y3Uhzz`); (2) la tarjeta de Categorías implementaba un acordeón inline no diseñado en vez de la lista plana + link final "Ver subcategorías" (`A3zxf`, `qrzfK`/`guwa6`) — se quitó el acordeón (incl. estado `expandedCategoryIds` del cubit) y se dejó el link inerte con TODO explícito porque su destino de navegación aún no está diseñado (pendiente #3 de `graficas.md`); (3) el estado vacío de Flujo envolvía el `Empty State` en una `Report Card` y agregaba el toggle "Separar movimientos de deuda", ninguno presente en el frame `NPXSP`/`TWoDk`. Los 3 corregidos y **re-verificados RESUELTO** contra Pencil en la segunda pasada. Migración de Patrimonio de barras a `LineChart` confirmada como divergencia deliberada del `.pen` (que solo dibuja barras), siguiendo la nota escrita `khZjH`, no hallazgo. **Gap de cobertura no bloqueante** (por qué queda en 🟡 y no en ✅): 6 estados (`dashboard: carga`, `net worth: carga/vacío/historial insuficiente`, `categories: carga/vacío`) reutilizan consistentemente el lenguaje visual de skeleton/vacío ya validado en Flujo, pero no tienen frame propio construido en Pencil — pendiente de diseño explícito (o de documentar formalmente que Flujo es la referencia patrón para los otros 3 tabs), no de implementación. Sheet Selector de Periodo (`Sy92N`) sin variante oscura por diseño (documentado, no gap). **Corrida 2026-07-30 (`graficas-cerrar-pendientes-categorias`): APROBADA — 0 hallazgos** en la pieza nueva (drill-down in-place de subcategorías con pill "Atrás", tap-toggle con selección persistente en el donut mostrando nombre+monto en el centro, navegación de una fila de categoría/subcategoría a Movimientos filtrado por `categoryId`+rango de fechas). Cierra el pendiente #3 de `graficas.md` ("Ver subcategorías" sin destino diseñado) con el drill-down in-place + navegación a Movimientos como el diseño ya cerrado. **Segunda corrida de fidelidad (2026-07-30, tras aprobación explícita del claro):** `design-system/billetudo/pages/graficas.md` y `billetudo.pen` (canónico en `/Users/cami/Developer/Personal/billetudo/billetudo.pen`, no el checkout de este worktree — ver [[pen_canonico_vive_en_billetudo_no_graficas]]) **actualizados**: nueva sección "Categorías — interacción de la dona y drill-down", pill de drill-down componentizado como `Chart Drill Pill` (`OiM6E`, reusable:true, 3 estados vía overrides) reemplazando 3 copias inline, y tema oscuro construido para los 2 frames nuevos (`tCOi4`→`igjiR`, `d2bv47`→`KmgyI`) — revisado por `ui-ux-reviewer` sin CRÍTICO/IMPORTANTE (1 MENOR de documentación en `MASTER.md` sobre uso de `$coral`, corregido). Comparación código-vs-Pencil de esta pieza encontró **2 CRÍTICO + 1 IMPORTANTE reales** (no de diseño): "Mayor gasto" y el subtítulo de conteo no se recalculaban al entrar al nivel de subcategorías (seguían mostrando el dato de la raíz), y el pill inerte usaba `textSecondary`+`Opacity(0.5)` en vez del token `$segment-inactive-text` ya definido. Los 3 corregidos en `category_breakdown_card_content.dart`/`category_view_subcategories_link.dart` (+ nueva clave ICU en `app_es.arb`/`app_en.arb` para "subcategoría(s)"), más un ajuste de fixture de test (subcategorías con mismo color del padre, íconos distintos entre sí, decisión del usuario) — **re-verificados RESUELTOS** contra `d2bv47`/`KmgyI`/`OiM6E` en una segunda pasada puntual. Gaps señalados sin cambio: los mismos 6 estados de carga/vacío sin fila propia ya documentados arriba (Sheet Selector de Periodo oscuro incluido), y falta agregar filas propias para `tCOi4`/`igjiR`/`d2bv47`/`KmgyI` en la tabla "Vistas" de `graficas.md` (hoy solo está la fila raíz `A3zxf`/`Zyd8k`) — la feature se mantiene en 🟡 Parcial hasta que estos gaps de cobertura se cierren. |
| Captura, Improvement | ⬜️ N/A | — | ❌ | — | Sin implementación en `lib/features/` (solo `.gitkeep`) o sin pantallas propias todavía — no aplica auditoría de fidelidad. |
| Core (widgets compartidos, `lib/core/widgets/`) | ⬜️ N/A | 2026-07-29 | ❌ | `docs/dev-runs/unify-ledger-load-more.md` | No existe `design-system/billetudo/pages/core.md` (verificado con Glob), así que no hay spec por pantalla que mapee goldens a nodeId de Pencil, y tampoco existe `test/features/core/presentation/golden/goldens/` con `.png` generados — no hay nada contra qué comparar todavía. No es fallo de fidelidad: falta (a) el spec `pages/core.md` con la tabla Pantalla/pieza → Node ID Claro/Oscuro y (b) que `qa-automator` genere los goldens correspondientes. Origen: se agregó `LoadMoreButton` compartido en esta corrida (fiel al `BudgetLoadMoreButton` ya aprobado, no a un frame nuevo). |
| Core / notes (`lib/core/notes/`, `NoteAutocompleteField`) | ❌ Sin auditar | 2026-08-04 | ✅ (`test/core/widgets/note_autocomplete_field_test.dart`, 6 goldens default/scroll/no_matches × claro/oscuro; +30 goldens regenerados en los 5 sitios de consumo) | `docs/dev-runs/note-autocomplete.md` | **N/A** — no existe `design-system/billetudo/pages/core/notes.md` ni `design-system/billetudo/pages/notes.md` (confirmado con Glob completo sobre `design-system/billetudo/pages/**`). Sin ese `.md` no hay tabla Pantalla/pieza → Node ID contra la cual resolver los goldens de `NoteAutocompleteField` — gap de documentación, no fallo de fidelidad: el spec de Pencil ya existe como frames sueltos (`taqc1`/`otjW9`/`NdecZ` claro, `LOnm3`/`ETP5J`/`kS16A` oscuro) pero no está volcado a un `.md` de la feature. No se intentó `get_editor_state` porque sin el `.md` no habría mapeo válido nodeId↔golden. Pendiente: crear `design-system/billetudo/pages/core/notes.md` y correr `/design-fidelity-check` sobre los 5 sitios migrados antes de poder cerrar esta fila con un estado real. |

## Pendientes activos (backlog, `docs/bugfixes.md` al 2026-07-20)

Ninguno activo al cierre de la sesión del 2026-07-20 — los 3 pendientes que quedaban (Cuentas,
hero con presupuesto, copy de error de Transacciones) se resolvieron en esta misma sesión.

Metas (2026-07-28): todos los hallazgos de la auditoría inicial se corrigieron en la misma
sesión — ver fila "Metas" arriba para el detalle completo. Ninguno activo.

## Lecciones ya documentadas para las próximas corridas

Ver `docs/dev-runs/fidelidad-presupuestos-PROMPT.md` (sección "Lecciones de la corrida de Pagos programados") — aplican a cualquier feature nueva que se audite: tandas secuenciales (no paralelas) por compartir `.arb`, re-verificar al final con `pencil-fidelity-reviewer`, preguntar puntualmente contra el `.pen` en vez de que `flutter-dev` adivine, acotar el alcance de cambios transversales, y revisar gaps de cobertura golden tanto como hallazgos.
